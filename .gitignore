# --- TypeScript / JavaScript (Node.js) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Salida de compilación (si usas Webpack, esbuild o Babel)
dist/
build/
lib-dist/

# --- xk6 (Extensiones personalizadas) ---
# Binarios personalizados de k6 generados con xk6
k6
k6.exe

# --- Entorno y Secretos ---
# Archivos de variables de entorno (¡Nunca subas credenciales!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- IDEs ---
.vscode/